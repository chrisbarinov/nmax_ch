# frozen_string_literal: true

RSpec.describe NmaxCh do
  it "has a version number" do
    expect(NmaxCh::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
